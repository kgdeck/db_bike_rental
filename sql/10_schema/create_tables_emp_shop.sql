CREATE TABLE EMPLO (
   ID INT PRIMARY KEY,
   NAME VARCHAR(100),
   SHOP INT,
   HIRE_DATE DATE,
   JOB VARCHAR(100),
   WORKLOAD NUMERIC(10, 2),
   SALARY INT,
   ADDRESS INT,
   REPORTS_TO INT,
   FOREIGN KEY (REPORTS_TO) REFERENCES EMPLO(ID),
   CHECK (WORKLOAD BETWEEN 0.0 AND 1.0)
);

CREATE TABLE SHOP (
   ID INT PRIMARY KEY,
   NAME VARCHAR(100),
   ADDRESS INT,
   MANAGER INT
);

ALTER TABLE SHOP ADD CONSTRAINT FK_SHOP_MANAGER FOREIGN KEY (MANAGER) REFERENCES EMPLO(ID);
ALTER TABLE EMP ADD CONSTRAINT FK_EMPLO_SHOP FOREIGN KEY (SHOP) REFERENCES SHOP(ID);
